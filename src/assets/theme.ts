import { Theme } from "@aws-amplify/ui-react";

export const myTheme: Theme = {
    name: 'my-theme',
    tokens: {
      colors: {
        font: {
          primary: { value: 'red' },
        },
      },
    },
  };